# W6 validation requests

Validator: W6 (W6.VALIDATE.01), candidate `ed8048c` (`feat/floodline-slice`).
These are requests to path owners. W6 did not edit implementation code. Each request says what was tested without the hook and what remains untested.

| # | Owner | Request | Why | Tested without it |
| --- | --- | --- | --- | --- |
| VR1 | C1 (`src/app/**`, `src/main.ts`) | Expose the renderer attempt (`webgpu` / `webgpu-webgl2` / `webgl2-classic`) and init notes on `window.__floodline`, e.g. `renderer(): { backend, attempt, notes }`. | `backend()` returns `'webgl2'` for both the three WebGPURenderer WebGL2 backend and the classic WebGLRenderer, so R03 cannot identify the path from the app. | Yes. A test-side `getContext` wrapper attributes the game-canvas context to the calling chunk (`three.webgpu-*.js` or `index-*.js`); see `tests/e2e/gl-probe.ts`. |
| VR2 | C1 | Expose `RendererAdapter.stats()` (read-only) on the debug handle. | R02 and bench receipts should record draw calls, triangles, CPU frame time, internal scale and quality tier. | Partly. `bench:frame` records page rAF intervals only, and `rendererStats` is `null` in `reports/bench-frame.json`. |
| VR3 | C1 / W2 | Provide a supported way to force the classic `WebGLRenderer`, for example `?renderer=webgl2-classic` or a settings value. | The `renderer: 'webgl2'` setting still tries WebGPURenderer's WebGL2 backend first, so the strict-exit fallback path (ADR 0002) can be selected only by making that backend fail. | Yes, by withholding the first WebGL2 context the three.webgpu chunk requests on the game canvas (R03 variant C, bench `webgl-renderer-classic`). |
| VR4 | C1 (`src/app/game.ts` `initRenderer`) | After a device or context loss, recover on a fresh canvas, not only when the canvas held WebGPU. | R05 **fails**. After `WEBGL_lose_context.loseContext()`, `initRenderer('webgl2')` reuses the lost canvas. `webgpu-webgl2` fails with `Cannot read properties of null (reading '0')`, `webgl2-classic` fails with `(reading 'precision')`, the backend stays `'none'` and the fatal screen appears. Retry on the still-lost context also fails. | Yes: `tests/e2e/renderer-failure.spec.ts`, evidence `reports/e2e/r05-context-loss-*.json`. The simulation keeps ticking throughout. |
| VR5 | W4 (`src/ui/ui.ts`) | Dismiss the fatal overlay when a later renderer init succeeds; for example `GameUi.clearFatal()`, called by C1 on a successful re-init. | After `restoreContext()` and Retry, `backend()` returns to `'webgl2'`, but the fatal dialog stays on screen with the old error and covers the game. | Yes: `afterRestoreContextAndRetry` in `reports/e2e/r05-context-loss-recovery.json`. |
| VR6 | W4 (`src/ui/input/core.ts`) or W1 | Decide whether pressing Shift (F) outside an anchor should reach the simulation (so it emits `ShiftRejected`) or show UI feedback. | ADR 0004's failure trace has the engine reject a shift requested outside an anchor. With real input, `shiftAnchorId` is `null` outside an anchor, so no request is made, nothing is rejected and there is no feedback. State stays intact. | Yes: `tests/e2e/real-input.spec.ts` (real F press: state intact, **no** `ShiftRejected`; direct sim command: `ShiftRejected`). |
| VR7 | C1 | Let `control()` on the debug handle, or a documented e2e hook, reach the simulation in `?demo` mode. Alternatively, document that it is a no-op there. | The autopilot input's `queueControl` is a no-op, so in demo mode `window.__floodline.control(...)` silently does nothing. | Yes. The G04 and W04 control-screen tests run in normal play: a demo creates the real `cp-control` save, then Continue is used. |
| VR8 | C1 | Expose `SemanticAdapter.receipt()` (module, wasm sha256, init ms, node and edge counts) on the debug handle. | W03 and the bench receipt should carry the in-browser WASM init time and the projected graph size. | No. Only `semanticStatus()` is observable. The WASM hash comes from `src/wasm/pkg/RECEIPT.json` and from `dist/` via `audit:assets`. |
